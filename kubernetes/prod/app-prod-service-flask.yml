### app-prod-service-flask.yml
apiVersion: v1
kind: Service
metadata:
  name: flask-api-service-prod
spec:
  selector:
    app: flask-api
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000